// Site-ul foloseste output: "export" (static) — middleware nu ruleaza pe GitHub Pages.
// Blocarea site-ului e implementata client-side in layout.tsx cu localStorage.
export function middleware() {}
export const config = { matcher: [] };
