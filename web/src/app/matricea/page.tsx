import { redirect } from 'next/navigation';

// Redenumit in Temelia — pastram ruta veche functionala pentru linkuri/SEO existente.
export default function MatriceaPage() {
  redirect('/temelia/');
}
